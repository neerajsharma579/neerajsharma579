import { AppIntegerInputDirective } from './app-integer-input.directive';

describe('AppIntegerInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppIntegerInputDirective();
    expect(directive).toBeTruthy();
  });
});
